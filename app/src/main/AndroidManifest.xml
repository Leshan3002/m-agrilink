<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- STEP 1: MANIFEST PERMISSIONS INJECTION -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <queries>
        <!-- TASK 1: Verified Kenyan Agricultural Package Namespaces -->
        <package android:name="ke.or.kalro.kaop" />
        <package android:name="ke.or.kalro.varietyselector" />
        <package android:name="ke.or.kamis" />

        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="market" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MAgriLink">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MAgriLink"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Registered Sub-Activity for National Market Corridor Engine -->
        <activity
            android:name=".NationalMarketActivity"
            android:exported="false"
            android:label="National Market Corridor Engine"
            android:theme="@style/Theme.MAgriLink" />

        <!-- Registered Sub-Activity for In-App Market Arbitrage Analyzer -->
        <activity
            android:name=".MarketAnalyzerSubpageActivity"
            android:exported="false"
            android:label="Cross-Country Market Price Analyzer"
            android:theme="@style/Theme.MAgriLink" />

        <!-- Registered Sub-Activity for In-App Native Weather Forecast -->
        <activity
            android:name=".WeatherForecastActivity"
            android:exported="false"
            android:label="Weather Forecast Hub"
            android:theme="@style/Theme.MAgriLink" />

        <!-- Registered Sub-Activity for In-App Farm Boundary Profiler Maps -->
        <activity
            android:name=".FarmProfilerActivity"
            android:exported="false"
            android:label="Farm Boundary Profiler"
            android:theme="@style/Theme.MAgriLink" />

    </application>

</manifest>
